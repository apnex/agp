{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agp:schema:v1:protocol:common:node-id",
  "title": "NodeId",
  "description": "Canonical lowercase identity of one AGP node.",
  "x-agp": {
    "owner": "@agp/protocol",
    "typescript": "NodeId",
    "kind": "scalar",
    "mechanics": "Canonical lowercase identity of one AGP node.",
    "rationale": "A sovereign contract gives validators and consumers one stable reasoning boundary.",
    "consequence": "Accepting another shape would make peer interpretation or ownership ambiguous.",
    "semanticRules": []
  },
  "type": "string",
  "minLength": 1,
  "maxLength": 128,
  "pattern": "^[a-z0-9][a-z0-9._-]{0,127}$"
}
