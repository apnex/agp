{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agp:schema:v1:transport-loopback:common:loopback-address",
  "title": "AGP Loopback loopback-address",
  "x-agp": {
    "owner": "@agp/transport-loopback",
    "typescript": "LoopbackAddress",
    "kind": "common",
    "mechanics": "Sovereign loopback-address data contract owned by @agp/transport-loopback.",
    "rationale": "One closed record keeps production Loopback configuration and state independently inspectable.",
    "consequence": "Accepting another shape would make fabric capacity, lifecycle, or terminal evidence ambiguous.",
    "semanticRules": []
  },
  "description": "Exact slash-separated listener address inside one fabric.",
  "type": "string",
  "pattern": "^[a-z0-9][a-z0-9._-]{0,62}(?:/[a-z0-9][a-z0-9._-]{0,62})*$",
  "maxLength": 253
}
