{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agp:schema:v1:transport:contracts:transport-diagnostic",
  "title": "TransportDiagnostic",
  "description": "Bounded sanitized adapter diagnostic safe at the neutral boundary.",
  "x-agp": {
    "owner": "@agp/transport",
    "typescript": "TransportDiagnostic",
    "kind": "contract",
    "mechanics": "Defines the bounded transport-diagnostic value crossing the neutral transport boundary.",
    "rationale": "One schema and generated type prevent adapter-specific reinterpretation.",
    "consequence": "A divergent shape makes transport implementations observably incompatible.",
    "semanticRules": []
  },
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9_]{0,63}$",
      "description": "Stable diagnostic code."
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "pattern": "^[^\\u0000-\\u001F\\u007F]+$",
      "description": "Optional sanitized diagnostic text."
    }
  },
  "required": [
    "code"
  ],
  "additionalProperties": false
}
